# CivitAI Download Manager - Requirements
# This file lists all the dependencies needed to run the CivitAI Download Manager

# GUI Framework - PyQt5 is the main UI framework
PyQt5>=5.15.0

# HTTP Requests - For API calls to CivitAI
requests>=2.28.0

# Secure credential storage (uses Windows Credential Manager backend on Windows)
keyring>=24.0.0

# Image Processing - For image handling, resizing, and format conversion
Pillow>=9.0.0

# Core Python Libraries (usually included with Python, but listed for completeness)
# These are typically part of the standard library but may need explicit installation in some environments

# Standard library modules used:
# - sqlite3 (database)
# - json (data serialization)
# - os (operating system interface)
# - sys (system parameters)
# - re (regular expressions)
# - math (mathematical functions) 
# - threading (thread support)
# - datetime (date and time handling)
# - hashlib (hash algorithms)
# - urllib.parse (URL parsing)
# - pathlib (object-oriented filesystem paths)
# - io (input/output operations)
# - tempfile (temporary files)
# - shutil (high-level file operations)

# Optional but recommended:
# - typing (type hints support for older Python versions)

# Installation Notes:
# 1. Make sure you have Python 3.7 or later installed
# 2. Install dependencies using: pip install -r requirements.txt
# 3. On some systems you may need to install PyQt5 differently:
#    - Windows: pip install PyQt5
#    - macOS: pip install PyQt5 or brew install pyqt5
#    - Linux: sudo apt install python3-pyqt5 or pip install PyQt5
# 4. If you encounter issues with PyQt5, try installing it via conda:
#    conda install pyqt=5

# Compatibility:
# - Tested with Python 3.8+
# - Should work with Python 3.7+ 
# - PyQt5 is compatible with Python 3.6+
